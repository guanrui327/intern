# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# 环境
.env
venv/
.venv/

# 原始数据（大文件，不进git）
data/

# 产出（可复现，不提交）
output/

# 记忆系统（会话持久化，不提交）
memory/

# 一次性调试脚本（本地归档，不入库）
_archive/

# 本地会话配置（堆临时权限白名单，不提交）
.claude/settings.local.json
